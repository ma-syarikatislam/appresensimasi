function handleMenu(menuName) {
  alert("Menu dipilih: " + menuName);
  // Ganti dengan navigasi sebenarnya, misalnya:
  // window.location.href = `./${menuName.replace(/\s/g, '_').toLowerCase()}.html`;
}
